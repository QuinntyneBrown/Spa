<div class="{{ prefix }}-heading-group">
  <h1 class="{{ prefix }}-panel-heading">{{ entityNamePascalCase }} List</h1>
  <div class="{{ prefix }}-heading-group__actions-container">
    <button class="{{ prefix }}-button" (click)="create.emit()">
      Create
    </button>
  </div>
</div>

<table mat-table [dataSource]="dataSource" class="{{ prefix }}-table" style="--grid-template-columns: 1fr auto;">

  <ng-container matColumnDef="name">
    <th mat-header-cell *matHeaderCellDef> Name </th>
    <td mat-cell *matCellDef="let {{ entityNameCamelCase }}"> {{ openDoubleSquilly }} {{ entityNameCamelCase }}.name {{ closeDoubleSquilly }}</td>
  </ng-container>

  <ng-container matColumnDef="actions">
    <th mat-header-cell *matHeaderCellDef></th>
    <td mat-cell *matCellDef="let {{ entityNameCamelCase }}">
      <button mat-icon-button (click)="select.emit({{ entityNameCamelCase }})" [disabled]="selected?.{{ entityNameCamelCase }}Id === {{ entityNameCamelCase }}.{{ entityNameCamelCase }}Id">
        <mat-icon>edit</mat-icon>
      </button>
      <button mat-icon-button (click)="delete.emit({{ entityNameCamelCase }})" [disabled]="selected?.{{ entityNameCamelCase }}Id === {{ entityNameCamelCase }}.{{ entityNameCamelCase }}Id">
        <mat-icon>delete</mat-icon>
      </button>
    </td>
  </ng-container>

  <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
  <tr mat-row *matRowDef="let {{ entityNameCamelCase }}; columns: displayedColumns;"></tr>

</table>

<mat-paginator class="{{ prefix }}-paginator"
  [pageSizeOptions]="pageSizeOptions">
</mat-paginator>
