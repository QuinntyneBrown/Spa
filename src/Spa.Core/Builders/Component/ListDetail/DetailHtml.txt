<h1 *ngIf="!{{ entityNameCamelCase }}.{{ entityNameCamelCase }}Id" class="{{ prefix }}-panel-heading">Create {{ entityNamePascalCase }}</h1>
<h1 *ngIf="{{ entityNameCamelCase }}.{{ entityNameCamelCase }}Id" class="{{ prefix }}-panel-heading">Edit {{ entityNamePascalCase }}</h1>


<form [formGroup]="form" class="{{ prefix }}-form">
  <div class="{{ prefix }}-field">
    <label class="{{ prefix }}-label">Name</label>
    <input type="text" formControlName="name" autocomplete="off" class="{{ prefix }}-input">
  </div>
</form>

<div class="{{ prefix }}-actions">
  <button class="{{ prefix }}-button" (click)="save.emit(form.value)" [disabled]="!form.valid || form.pristine">
    Save
  </button>
</div>
